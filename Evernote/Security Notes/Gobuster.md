---
---
A quick gobuster command looks like this:

**_sudo gobuster dir -u <http://10.0.2.4> -w /usr/share/wordlists/dirb/common.txt -e_**

**a** longer attack will likely just use a longer wordlist:
