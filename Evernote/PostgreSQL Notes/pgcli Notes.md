---
---
* install this with **pip -U install pgcli** 

* get access to a database with the command **pgcli \[whatever database name\] -U postgres**
	* in the above command, we're using pgcli to open a given database as the user postgres
		* without specifying the latter, pgcli will default to the main Windows user, which is what you don't want
