---
---
# Syntax Errors v. Type errors

in type script, code must have valid syntax and must be type checked

* js: code → syntax check → execution
* Typescript: code → syntax check → type check → execution

the above means that if syntax is wrong, TS won't even bother running a type check on our code
