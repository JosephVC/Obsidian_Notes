---
---
**Defaults**

given the quantity of columns that need to be added to some databases, we can use default values to make the process a bit more efficient![[./_resources/Untitled_Note.18.resources/sql - default values.png]]

It's important to know that the type must come after the column name.  

Below is an example where we create a table of user comments

![[./_resources/Untitled_Note.18.resources/sql - default values - comment example.png]]

![[./_resources/Untitled_Note.18.resources/sql - default 0 example.jpg]]
